# Scripts

Project scripts are currently exposed from the root `package.json`.

Add operational scripts here when they are needed outside npm scripts, for example backup, migration, or release helpers.
